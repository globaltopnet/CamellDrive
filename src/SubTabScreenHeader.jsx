import React from 'react';
import SubCustomHeader from './SubCustomHeader';

const SubTabScreenHeader = ({ title, navigation }) => {
  return (
    <>
      <SubCustomHeader title={title} navigation={navigation} />
    </>
  );
};

export default SubTabScreenHeader;