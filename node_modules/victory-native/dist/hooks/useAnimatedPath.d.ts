import type { SkPath } from "@shopify/react-native-skia";
import { type WithSpringConfig, type WithTimingConfig, type WithDecayConfig } from "react-native-reanimated";
export type PathAnimationConfig = ({
    type: "timing";
} & WithTimingConfig) | ({
    type: "spring";
} & WithSpringConfig) | ({
    type: "decay";
} & WithDecayConfig);
export declare const useAnimatedPath: (path: SkPath, animConfig?: PathAnimationConfig) => Readonly<{
    value: SkPath;
}>;
