"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useBarPath = void 0;
const React = __importStar(require("react"));
const react_native_skia_1 = require("@shopify/react-native-skia");
const createRoundedRectPath_1 = require("../../utils/createRoundedRectPath");
const useBarPath = (points, chartBounds, innerPadding = 0.2, roundedCorners) => {
    const barWidth = React.useMemo(() => {
        const domainWidth = chartBounds.right - chartBounds.left;
        const barWidth = ((1 - innerPadding) * domainWidth) / (points.length - 1);
        return barWidth;
    }, [chartBounds.left, chartBounds.right, innerPadding, points.length]);
    const path = React.useMemo(() => {
        const path = react_native_skia_1.Skia.Path.Make();
        points.forEach(({ x, y }) => {
            if (typeof y !== "number")
                return;
            if (!roundedCorners) {
                path.addRect(react_native_skia_1.Skia.XYWHRect(x - barWidth / 2, y, barWidth, chartBounds.bottom - y));
                return;
            }
            const roundedRectPath = react_native_skia_1.Skia.Path.MakeFromSVGString((0, createRoundedRectPath_1.createRoundedRectPath)(x - barWidth / 2, y, barWidth, chartBounds.bottom - y, roundedCorners));
            roundedRectPath && path.addPath(roundedRectPath);
        });
        return path;
    }, [barWidth, chartBounds.bottom, points, roundedCorners]);
    return { path, barWidth };
};
exports.useBarPath = useBarPath;
